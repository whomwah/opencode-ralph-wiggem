{
  "name": "opencode-ralph-wiggum",
  "version": "1.0.0",
  "description": "Ralph Wiggum iterative development loop plugin for OpenCode",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "build:types": "tsc --emitDeclarationOnly",
    "prepublishOnly": "bun run build && bun run build:types",
    "dev": "bun run build --watch",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "link:local": "mkdir -p ~/.config/opencode/plugin && ln -sf $(pwd)/dist/index.js ~/.config/opencode/plugin/ralph-wiggum.js",
    "link:project": "mkdir -p .opencode/plugin && ln -sf $(pwd)/dist/index.js .opencode/plugin/ralph-wiggum.js",
    "unlink:local": "rm -f ~/.config/opencode/plugin/ralph-wiggum.js",
    "unlink:project": "rm -f .opencode/plugin/ralph-wiggum.js"
  },
  "keywords": [
    "opencode",
    "opencode-plugin",
    "ralph-wiggum",
    "ai",
    "iterative-development",
    "autonomous-coding"
  ],
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/whomwah/ralph-wiggum-opencode"
  },
  "bugs": {
    "url": "https://github.com/whomwah/ralph-wiggum-opencode/issues"
  },
  "homepage": "https://github.com/whomwah/ralph-wiggum-opencode#readme",
  "devDependencies": {
    "@opencode-ai/plugin": "latest",
    "@types/bun": "latest",
    "prettier": "^3.7.4",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=0.1.0"
  }
}
